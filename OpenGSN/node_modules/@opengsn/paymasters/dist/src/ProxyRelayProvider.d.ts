import { RelayClient, JsonRpcCallback, RelayProvider, GSNUnresolvedConstructorInput } from '@opengsn/provider';
import { Address } from '@opengsn/common/dist/types/Aliases';
import { JsonRpcPayload } from 'web3-core-helpers';
export default class ProxyRelayProvider extends RelayProvider {
    private readonly proxyFactoryAddress;
    constructor(proxyFactoryAddress: Address, relayClient: RelayClient);
    static newProvider(input: GSNUnresolvedConstructorInput): RelayProvider;
    static newProxyRelayProvider(proxyFactoryAddress: Address, input: GSNUnresolvedConstructorInput): ProxyRelayProvider;
    _ethSendTransaction(payload: JsonRpcPayload, callback: JsonRpcCallback): void;
    calculateProxyAddress(owner: Address): Promise<Address>;
}
